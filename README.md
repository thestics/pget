# PGET simple clone of WGET written in python


## Install
```bash
python3 -m venv venv
source venv/bin/activate
pip3 install git+ssh://git@github.com/thestics/pget.git
```

## Usage
```bash
pget [input-file] [existing-output-dir]
```